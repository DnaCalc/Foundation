# Open Items and Follow-up

## P0 Follow-up (Required Before Treating Provisional Lanes as Settled)
1. Expand parser ambiguity matrix around `XLS-CF-FL-010` (`EMP-0001`) across additional builds/channels.
2. Expand SUMIF reason-code matrix for `XLS-CF-FN-009` (`EMP-0009`) with criteria/range-shape variants.
3. Expand aggregate mixed-range coercion matrix for `XLS-CF-TV-008` (`EMP-0003`) with locale and compatibility-version axes.
4. Expand CF spill-target interaction probes for `XLS-CF-FM-005` (`EMP-0004`) with isolated rule-order/applies-to variants.
5. Expand table spill-growth variants for `XLS-CF-TB-004` (`EMP-0005`) across structured-ref forms.

## P1 Follow-up (Depth and Coverage Hardening)
1. Add full function-edge cases by tier using requirement-id bound scenario registries.
2. Add build/channel snapshots for platform-availability matrix refresh.
3. Add cross-platform replay where infrastructure is available (Windows/Mac/Web lanes).

## Process Follow-up
1. Bind existing and new conformance tests to `XLS-CF-*` ids in evidence bundles.
2. Promote additional empirical findings to `EMP-*` only when they satisfy promotion criteria.
3. Keep provisional lanes explicit; do not silently fold into normative rows.
