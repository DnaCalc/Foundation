Start a new research project to determine and document the overall DnaCalc project progress. For each in-scope `Ox*` repo that you find under DnaCalc (excluding Foundation and excluding DnaVisiCalc) ascertain the current status - what is the intended scope (according to the CHARTER and other documents of that repo) what have we actually done so far, what are outstanding work items, what do the current project outputs look like. To this end you have full read access to all repos, and you can make copies of repo outputs to put together test artifacts to see what the status is. Review the code, the current state of output artifacts, the head documents and outstanding work items.

After this review, and based on it, we will do some rounds of focused work in each repo and then do a giant clean-up of each, and the overall project. Prepare for this by gathering the best set of information you can.

Consider that this is a project in progress, and we have some aspects quite advanced, other are nascent. Gather the at-this-stage situation update, on which we can base further plans. Be sure to also gather executable evidence, documents may deceive in what completeness is claimed, so trust but verify, looking at the docs, the code and the current output artifacts.
