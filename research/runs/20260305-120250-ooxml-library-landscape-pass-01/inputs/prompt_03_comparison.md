Cross-ecosystem comparison prompt.

Compare Rust candidates with selected non-Rust libraries frequently used for OOXML spreadsheet handling.

Produce:
1) Comparative scoring matrix (weights + rationale).
2) Interop risk notes for Excel fidelity work.
3) Which libraries are suitable as:
   - primary implementation dependency,
   - file adapter helper,
   - differential testing oracle.
4) Licensing compatibility notes for DnaCalc usage.