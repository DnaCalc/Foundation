User intent:
- Produce a new synthesis pass that locks in the best current core engine plan.
- Put a full new projects/repo layout plan in place, including future host progression.

Required outcomes:
1. Core engine plan is explicit and staged:
   - Option-B-style layered model (immutable structural truth + runtime overlays + FEC/F3E seam),
   - staged concurrency and adoption waves,
   - required obligations/packs and decision points.
2. Program layout is explicit and operational:
   - component repo map (Foundation, DnaVisiCalc, OxFunc, OxFml, OxCalc, OxVba),
   - host/project map and progression (DNA OneCalc, DNA TreeCalc, DNA PreCalc, future DNA SuperCalc and DNA Calc),
   - dependency constitution and handoff rules.
3. Promotions are made into source-of-truth docs with decision records.

Constraints:
- Respect existing doctrine and precedence.
- Preserve Round 0 pathfinder authority in DnaVisiCalc docs.
- Avoid introducing date/ETA commitments; sequence-only planning.