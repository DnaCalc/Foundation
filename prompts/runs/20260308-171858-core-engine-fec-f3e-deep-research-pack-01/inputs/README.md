# Curated Inputs

This is the active curated `inputs/` directory.

Intent:
- keep only current, design-relevant material,
- reduce pack size for fast deep-research iteration,
- center current FEC/F3E spec plus compact implementation/evidence context.

Active files:
- `source/` compact corpus (**19 source files**)
- `source_index.csv` compact mapping
- `source_hashes.csv` compact frozen hashes
- `prompt_pro_deep_research_core_engine_fec_f3e_design.md` execution prompt

Superseded material:
- `retired/` holds files moved out of the active compact pack.
- `retired/` is preserved for traceability but should not be used as active synthesis input.
