Run: `DNA OneCalc` upstream clarification pass 02

Scope:
- Execute repo-local documentation clarification prompts for `OxCalc`, `OxFml`, and `OxFunc`.
- Record pre-run and post-run git cleanliness.
- Capture raw Claude responses and extracted human-readable summaries.

Repos:
- `C:\Work\DnaCalc\OxCalc`
- `C:\Work\DnaCalc\OxFml`
- `C:\Work\DnaCalc\OxFunc`

Outputs:
- `inputs/` prompt files used for each repo
- `logs/` raw Claude JSON output
- `responses/` extracted summaries or failure notes

Pre-run cleanliness:
- `OxCalc` was clean.
- `OxFml` was clean.
- `OxFunc` was clean at the time of execution.

Post-run outcome:
- `OxCalc` now has documentation edits in the seam-reference surface.
- `OxFml` now has documentation edits plus one new OneCalc-facing downstream contract note.
- `OxFunc` now has documentation edits plus two new downstream contract notes.
